name: keep server awake

on:
  schedule:
    - cron: "*/12 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: curl -fsS -o /dev/null "${{ secrets.RENDER_KEEP_ALIVE_URL }}" || true
