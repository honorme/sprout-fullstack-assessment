{
  "name": "sprout-fullstack-assessment",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "mocha tests/**/*.test.js --timeout 15000"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "concurrently": "^9.1.2",
    "dotenv": "^16.4.5",
    "mocha": "^7.2.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
